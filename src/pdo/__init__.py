"""PDO — Product Description Optimizer.

A CLI application that optimizes product descriptions via a daemon/client
architecture.  The lightweight ``pdo`` CLI connects to a long-running
``pdod`` daemon that handles CSV import, description optimization, and
export.
"""

__version__ = "0.1.0"
